# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Required for /admin operations (server-side only).
# NEVER expose this key to the browser.

# Preferred (Supabase "Secret" API key). Name may vary by dashboard.
# Put the value in one of the vars below.
SUPABASE_SECRET_KEY=your-secret-api-key

# Alternative name (some dashboards label it like this)
# SUPABASE_SECRET_API_KEY=your-secret-api-key

# Legacy fallback (older Supabase projects)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
